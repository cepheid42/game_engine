//
// Created by cepheid on 11/8/24.
//

#ifndef ELECTROMAGNETICS_PARAM_H
#define ELECTROMAGNETICS_PARAM_H

constexpr size_t nPml = 10u;
constexpr size_t nHalo = 0u;

using fp_t = double;
constexpr size_t DIM = 2;
constexpr fp_t cfl = 0.95 / std::sqrt(static_cast<fp_t>(DIM));

enum class EMFace { X, Y, Z};
enum class EMSide { Lo, Hi};
enum class EMComponent { E, H };
enum class Derivative { DX, DY, DZ, NoOp };

using EMField = EMFace;

#endif //ELECTROMAGNETICS_PARAM_H
